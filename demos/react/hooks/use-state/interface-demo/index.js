/**
 * useState 将会根据参数 initialState 来返回一个记忆化版本的状态值和一个更新该状态值的函数。
 * @param {unknown} initialState 该状态值的初始值（该参数在代码同一位置，仅首次有效）。
 * @returns {[unknown, Function]} 一个状态值和一个更新该状态值的函数
 */
function useState(initialState = undefined) {}