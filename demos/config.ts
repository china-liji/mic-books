import { reactConfig } from './react/config';
import { typescriptConfig } from './typescript/config';
import { domConfig } from './dom/config';
import { micConfig } from './mic/config';

export const config = [
  // prefaceConfig,
  reactConfig,
  typescriptConfig,
  domConfig,
  micConfig,
];