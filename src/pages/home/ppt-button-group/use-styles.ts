import { createStyles } from 'mic-global';

export const usePptButtonGroupStyles = createStyles(
  'ppt-button-group',
  {
    '& .ant-radio-button-wrapper-checked': {
      // pointerEvents: 'none',
    },
  }
);